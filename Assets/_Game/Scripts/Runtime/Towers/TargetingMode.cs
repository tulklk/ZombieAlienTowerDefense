namespace AlienDefense.Towers
{
    /// <summary>Selects which ITargetingStrategy a tower uses.</summary>
    public enum TargetingMode
    {
        First = 0,
        Closest = 1,
        Strongest = 2
    }
}
