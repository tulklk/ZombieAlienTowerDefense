namespace AlienDefense.Combat
{
    /// <summary>Broad damage category used only to resolve defense counters (e.g. Armor vs Physical).</summary>
    public enum DamageType
    {
        Physical = 0,
        Energy = 1,
        Explosive = 2,
        True = 3
    }
}
